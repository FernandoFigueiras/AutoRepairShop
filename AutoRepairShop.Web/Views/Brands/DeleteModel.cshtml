@model AutoRepairShop.Web.Data.Entities.Model

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Model</h4>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.ModelName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ModelName)
        </dd>
    </dl>

    <form asp-action="DeleteModel">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        @*<a asp-action="Details" asp-route-id="@ViewBag.Id">Back to Details</a>*@
    </form>
    <div>
        <a asp-controller="Brands"  asp-action="Details" asp-route-id="@ViewBag.Id">Back to Details</a>
    </div>
</div>
